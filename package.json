{
  "name": "llm-chatbot",
  "version": "0.1.0",
  "private": true,
  "description": "Multi-tenant RAG customer-support chatbot (NestJS + Next.js + MongoDB Atlas + Claude).",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev:backend": "npm run start:dev --workspace backend",
    "dev:frontend": "npm run dev --workspace frontend",
    "build": "npm run build --workspace backend && npm run build --workspace frontend"
  }
}
